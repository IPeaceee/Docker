services:
  redis:
    image: redis
    environment:
      - REDIS_PASSWORD
    command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - 6379:6379
    volumes:
      - redis-db:/data

volumes:
  redis-db: